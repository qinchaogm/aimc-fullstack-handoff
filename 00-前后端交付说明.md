# AIMC 前端拆分交付说明

交付时间：2026-09-15

原单体 Vue 3 原型已按使用场景拆分为两个平级项目。两个项目保持相同技术栈和独立的依赖、路由、环境变量、构建产物及部署入口。

## 1. 项目边界

| 项目 | 默认端口 | 职责 |
| --- | ---: | --- |
| `aimc-fullstack-handoff` | 47321 | 官网、公开赛事内容、参赛用户登录注册、报名、需求征集、个人中心 |
| `aimc-ops-console` | 47322 | 管理员工作台、报名与材料审核、评委分配、评分、通知、反馈、账号权限、评委评审 |

推荐部署为两个独立站点，例如：

- 门户：`www.example.com`
- 运营后台：`ops.example.com`

门户通过 `VITE_OPS_URL` 跳转后台，后台通过 `VITE_PORTAL_URL` 返回门户。不要在两个项目之间直接引用源码文件；需要共用的领域协议应由后端 OpenAPI 或独立共享包维护。

## 2. 本地启动

环境要求：Node.js 20+、pnpm 10.33.3。

门户：

```bash
cd aimc-fullstack-handoff
pnpm install --frozen-lockfile
pnpm dev
```

运营后台：

```bash
cd ../aimc-ops-console
pnpm install --frozen-lockfile
pnpm dev
```

完整校验均使用：

```bash
pnpm check
```

## 3. 路由边界

门户路由：

- 公开：`/`、`/contest`、`/tracks/:key`
- 游客：`/login`、`/register`
- 参赛用户：`/apply`、`/demand`、`/dashboard`

运营后台路由：

- 登录：`/login`
- 管理员：`/admin/registrations`、`/admin/materials`、`/admin/assign`、`/admin/scores`、`/admin/notices`、`/admin/demands`、`/admin/feedback`、`/admin/accounts`
- 评委：`/judge`、`/judge/:id`
- 状态页：`/forbidden`、404

## 4. 后台菜单与权限

后台采用“角色 + 权限码”两层控制：

1. `src/auth/permissions.ts` 定义权限码和原型角色权限映射。
2. `src/config/navigation.ts` 为每个菜单声明所需权限。
3. 布局只展示当前账号有权访问的菜单。
4. 路由 `meta.permission` 再做一次页面访问校验，无权访问进入 403。
5. 接入真实后端后，由登录或 `/me` 接口返回权限码；接口本身仍必须在服务端鉴权。

前端菜单隐藏不是安全措施，不能替代服务端授权。

## 5. 演示账号

短信验证码固定为 `123456`。

| 站点 | 角色 | 手机号 | 密码 |
| --- | --- | --- | --- |
| 门户 | 参赛用户 | `13700000001` | `User@2026` |
| 运营后台 | 管理员 | `13800000000` | `Admin@2026` |
| 运营后台 | 评委 | `13900000001`～`13900000005` | `Judge@2026` |

管理员和评委账号不能进入门户业务页；参赛用户不能进入运营后台。

## 6. 后端接入边界

建议按领域提供接口：

| 领域 | 建议接口范围 |
| --- | --- |
| 认证与身份 | `/api/auth/*`、`/api/me`，返回用户、角色、状态和权限码 |
| 报名 | `/api/registrations` |
| 参赛材料 | `/api/materials`，文件落 OSS/MinIO |
| 评委分配 | `/api/assignments` |
| 评分 | `/api/reviews` |
| 站内信 | `/api/notices` |
| 需求征集 | `/api/demands` |
| 用户反馈 | `/api/feedback` |
| 账号权限 | `/api/users`、`/api/roles`、`/api/permissions` |

当前是本地 mock 原型。两个项目位于不同浏览器源，各自的 `localStorage` 数据不会同步，这是拆分后的预期限制；正式联调时应尽快以同一后端作为唯一业务数据源。

## 7. 工程维护约定

- 页面只放在各自项目的 `src/views`，不跨项目复制路由页面。
- 菜单配置、权限码、环境配置分别集中维护，避免散落硬编码。
- `pnpm-lock.yaml` 必须随源码提交，CI 使用 `pnpm install --frozen-lockfile`。
- 合并前两个项目分别执行 `pnpm check`。
- `node_modules/`、`dist/`、本机 `.env.local`、日志和 IDE 缓存不纳入交付。
